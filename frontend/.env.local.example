# Django API (no trailing slash). Required for blog, experiments, dashboard, and hire form.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Optional: shown on /hire as a mailto fallback when the API is not configured (e.g. static preview).
# NEXT_PUBLIC_CONTACT_EMAIL=hello@example.com
